# QuizSystem
The Interactive Quiz System is a desktop-based application built using the Java Swing and AWT libraries. It follows an event-driven programming model, meaning the program stays "idle" until a user interacts with it (like clicking a button) or a specific time interval passes (the timer)
